apiVersion: v1
kind: Service
metadata:
  name: pedidos-service
  namespace: sre-platform
  labels:
    app: pedidos-service
spec:
  selector:
    app: pedidos-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
